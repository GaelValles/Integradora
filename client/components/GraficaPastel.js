// import { PieChart } from 'react-native-chart-kit';
// import React from 'react';

// const GraficaPastel = ({ data, width, height, accessor }) => {
//   const chartConfig = {
//     backgroundColor: '#ffffff', // Color de fondo del gráfico
//     backgroundGradientFrom: '#eff3ff', // Gradiente de fondo (inicio)
//     backgroundGradientTo: '#efefef', // Gradiente de fondo (fin)
//     color: (opacity = 1) => `rgba(0, 0, 0, ${opacity})`, // Color de las etiquetas
//   };

//   return (
//     <PieChart
//       data={data}
//       width={width}
//       height={height}
//       chartConfig={chartConfig}
//       accessor={accessor}
//       backgroundColor="transparent" // Fondo transparente para la gráfica
//       paddingLeft="15" // Espacio a la izquierda del gráfico
//       absolute // Posición absoluta del gráfico
//     />
//   );
// };

// export default GraficaPastel;
